<!DOCTYPE html>
<%- include('./partials/head',{ title : 'Universidad' }) %>

    <body class="universities bg-light">
        <%- include('./partials/header') %>
            <main class="container">
                <section class="row mt-2 mt-md-3 mt-lg-5">
                    <div class="col-12 col-md-10 col-lg-8 mx-auto">
                        <article class="card shadow">
                            <div class="card-header">
                                <nav aria-label="breadcrumb">
                                    <ol class="breadcrumb">
                                        <li class="breadcrumb-item"><a href="/admin">Home</a></li>
                                        <li class="breadcrumb-item"><a href="universities">Universidades</a></li>
                                        <li class="breadcrumb-item active" aria-current="page"><%= name %></li>
                                    </ol>
                                </nav>
                                <hr>
                            </div>
                            <div class="card-body">
                                <div class="d-flex gap-3 flex-wrap justify-content-around align-items-center">
                                        <img class="img-fluid" width="150px" src="/images/logos/<%= image %>"
                                            alt="vista previa">
                                    <div>
                                        <div class="d-flex flex-column align-items-end">
                                            <a href="/universities/edit/<%= id %>" class="btn btn-sm btn-outline-success mb-3 ">Editar</a>
                                            <h3><%= acronym %> | <%= name %></h3>
                                        </div>
                                        <p class="text-muted">Estadísticas</p>

                                      
                                    </div>
                                </div>

                            </div>

                        </article>

                    </div>
                </section>

            </main>
            <%- include('./partials/scripts') %>
    </body>

    </html>